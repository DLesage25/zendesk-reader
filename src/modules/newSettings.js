const newSettings = {};

newSettings.program = (program) => {
	return {
		bambooId: 'updateThis',
		goal: 'touches',
		id: program,
		isActive: true,
		managerId: 'updateThis',
		prettyName: program
	}
}

export default newSettings;